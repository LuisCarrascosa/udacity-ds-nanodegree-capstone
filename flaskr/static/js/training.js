function add(row) {
    var row = document.getElementById(row.id);
    row.parentNode.removeChild(row);
}

function drop(row) {
    var row = document.getElementById(row.id);
    row.parentNode.removeChild(row);
}